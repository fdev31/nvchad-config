Configuration for https://nvchad.com/

# Changes

- changes `<leader>n` to toggle relative numbers
- use `<leader>rn` to rename instead of `<leader>ra`
- toggling numbers isn't available anymore (always on)
